{% extends 'base.html.twig' %}

{% block title %}Thème{% endblock %}

{% block body %}
    <h1>Thème</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom du Thème</th>

            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <a href="{{ path('app_quiz_show', {'id': category.id}) }}">Afficher</a>
                    <a href="{{ path('app_quiz_edit', {'id': category.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Pas de résultats</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_quiz_new') }}">Ajouter un Thème</a>
{% endblock %}