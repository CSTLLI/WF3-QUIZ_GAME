<!DOCTYPE html>
<html class="h-100">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">    

    <title>Quiz Game {% block title %}{% endblock %}</title>

    {# BLOCK CSS #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {# BLOCK JS #}
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

</head>

    {# HEADER #}
    <header>
      <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="container-fluid">

            <a class="navbar-brand ms-2 me-4" href="{{ path('app_main_base') }}">Quiz Game</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>


          <div class="collapse navbar-collapse" id="navbarCollapse">

            {# Menu de Gauche #}
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_result_index') }}"><i class="fa-solid fa-lg fa-newspaper pe-2"></i>Quiz</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fa-solid fa-lg fa-envelope pe-2"></i>######</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="fa-solid fa-lg fa-file-circle-plus pe-2"></i>#####</a>
                </li>              
            </ul>
        </div>
    </header>

<body class="d-flex flex-column h-100">


    <main>

      {# BLOCK PRINCIPAL #}
      {% block body %}{% endblock %}

    </main>        

    {# FOOTER #}
    <footer class="footer mt-auto py-2 bg-dark">
      <div class="container">
        <span class="text-muted">Ceci est le footer</span>
      </div>
    </footer>

</body>

</html>